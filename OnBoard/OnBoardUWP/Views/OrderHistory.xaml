<Page
    x:Class="OnBoardUWP.Views.OrderHistory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnBoardUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:OnBoardUWP.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Frame x:Name="orderFrame">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="10">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Orders" FontSize="30"></TextBlock>
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <ListView x:Name="myLv" ItemsSource="{x:Bind viewModel.Orders, Mode=OneWay}">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal"></ItemsWrapGrid>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:Order">
                                    <StackPanel Orientation="Vertical" Width="180">
                                        <TextBlock Text="{x:Bind OrderDetails}" FontWeight="Medium" TextAlignment="Center" FontSize="16"></TextBlock>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                                            <TextBlock Text="€" Margin="0,0,2,0"></TextBlock>
                                            <TextBlock Text="{x:Bind OrderState}"></TextBlock>
                                        </StackPanel>
                                        <Button x:Name="GoBack" Content="Back to catalog" Click="goBackToCatalog" Margin="0,14,0,40"></Button>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </StackPanel>

            </Grid>
        </ScrollViewer>
    </Frame>
</Page>
