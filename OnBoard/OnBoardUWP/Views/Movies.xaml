<Page
    x:Class="OnBoardUWP.Views.Movies"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnBoardUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <ScrollViewer>
        <Grid>
            <ItemsControl ItemsSource="{x:Bind movieViewModel.Movies}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!--<Grid x:Name="MovieGrid" Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                           
                        </Grid>-->

                        <ListView>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                        </ListView>
                        
                        <!--<StackPanel >
                            <Image Stretch="Fill" Width="auto" Height="auto">
                                <Image.Source>
                                    <BitmapImage UriSource="{Binding Poster}" ></BitmapImage>
                                </Image.Source>
                            </Image>
                            <TextBlock TextWrapping="Wrap" Margin="10" TextAlignment="Center" Text="{Binding Title}"></TextBlock>
                        </StackPanel>-->
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>

</Page>
